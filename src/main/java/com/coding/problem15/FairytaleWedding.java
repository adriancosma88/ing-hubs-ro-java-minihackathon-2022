package com.coding.problem15;


/**
 * A couple want to have a wedding like no other. Everything needs
 * to be out of the ordinary, thus they are planning a special means
 * of transport for taking their guests to restaurant.
 * <p>
 * They are planning to take all guests by alpacas drawn 2 persons
 * carriages. Though cute, alpacas are not that strong.
 * <p>
 * Each carriage can take at most 2 people and the sum of the weight
 * of those people is at most <code>limit</code>.
 * <p>
 * You are given an array people, where people[i] is the weight of the
 * i-th person.
 * Help the couple plan the wedding, by returning the minimum number
 * of alpacas to carry all the persons attending the event.
 * <p>
 * Example:
 * Input: peopleWeights = [3, 2, 2, 1], maxWeight = 3
 * Output: 3
 * 3 carriages: (1, 2), (2), (3).
 * <p>
 * people.length in the range [1..50_000]
 * people[i] in the range [1..limit] where limit in [1..30_000]
 */
public class FairytaleWedding {
    public int alpacasNecessary(int[] peopleWeights, int maxWeight) {
        return -1;
    }
}
